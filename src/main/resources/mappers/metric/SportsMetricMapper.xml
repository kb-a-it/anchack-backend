<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kbait.anchack.metric.mapper.SportsMetricMapper">

  <select id="findAllCountsFromPlaces" resultType="com.kbait.anchack.metric.dto.SportsMetricRow">
    SELECT
      ad.admin_dong_id,
      ad.dong_population,
      ad.dong_area,
      COUNT(p.place_id) AS sports_count
    FROM admin_dongs ad
    LEFT JOIN places p ON p.admin_dong_id = ad.admin_dong_id AND p.category = 'SPORTS'
    GROUP BY ad.admin_dong_id, ad.dong_population, ad.dong_area
  </select>

  <insert id="insertRows">
    INSERT INTO sports_metrics (admin_dong_id, sports_count, sports_score)
    VALUES
    <foreach collection="rows" item="row" separator=",">
      (#{row.adminDongId}, #{row.sportsCount}, #{row.score})
    </foreach>
  </insert>

</mapper>
